# Enclosure Acoustic Simulator Setup

This project is configured as a standard React + Vite + TypeScript application with Tailwind CSS formatting.

## Setup Instructions

1. Install dependencies:
   ```bash
   npm install
   ```

2. Run local development environment:
   ```bash
   npm run dev
   ```

3. Build for production distribution:
   ```bash
   npm run build
   ```
