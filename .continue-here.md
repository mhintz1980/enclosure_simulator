---
phase: initial-review
task: 2
total_tasks: 5
status: in_progress
last_updated: 2026-06-04T21:00:00Z
---

<current_state>
Reviewed full codebase of generator acoustic enclosure simulator (React + Vite + TypeScript + Tailwind). Found 9 issues across critical/functional/dead code categories. User moved original `src/` files to `src/archive/` and has new versions in `src/`. New `App.tsx` (capital A) fixes the import case mismatch and adds more UI input controls. User added `tsconfig.json` (confirmed valid). Bun 1.3.3 installed on system.

Project path: `/mnt/c/Projects/enclosure_simulator/generator_acoustic_simulator`
Stack: React 19, Vite 5, TypeScript 5.4, Tailwind 3.4
One commit: `4b3c99c feat: initial commit of generator acoustic enclosure simulator`
</current_state>

<completed_work>
- Task 1: Full codebase review of original `src/` files — cataloged 9 issues
- Task 2: Verified new `src/App.tsx` is different from archived `src/archive/app.tsx` — expanded UI controls, capital-A filename
- Task 3: Confirmed `tsconfig.json` is valid and correct for Vite + React stack
</completed_work>

<remaining_work>
- Task 3: Fix config filenames — `postcss_config.js` → `postcss.config.js`, `tailwind_config.js` → `tailwind.config.js` (Tailwind/PostCSS won't auto-discover underscore names)
- Task 4: Fix physics bug — pressure drop not recalculated after duct height clamping to 0.3m minimum in `solve-height` branch (L82-87 of App.tsx). Reported value can be wrong.
- Task 5: Evaluate new `App.tsx` fully — check if all 5 previously-missing input controls now exist, whether chat dead code was cleaned up, `hover:bg-slate-850` fix
- Task 6: Run `bun install` + `bun run dev` to verify app boots and Tailwind styles work after config renames
</remaining_work>

<decisions_made>
- tsconfig.json is good as-is (standard Vite+React template). No changes needed.
- Bun chosen as runtime (user installed 1.3.3) — can replace npm for dev/build.
- Import case mismatch resolved by user renaming file to `App.tsx` (capital A).
</decisions_made>

<blockers>
- Config filename underscores (`postcss_config.js`, `tailwind_config.js`) will prevent Tailwind from working at all. Must fix before any visual testing.
- `noUnusedLocals` / `noUnusedParameters` in tsconfig will cause `tsc` failures if App.tsx has unused imports (useRef, useEffect, chat state). Need to verify.
</blockers>

<context>
This is a generator enclosure acoustic simulator — calculates airflow, pressure drop, silencer profiles, and thermal requirements for diesel generator enclosures. Uses Price Industries silencer data and references Caterpillar standards. The user is an engineer, not a full-time developer. Keep fixes practical.

The app has a "Roadblock Solver" panel with a mocked AI chat. State exists but may not be wired to UI in current version. The physics solver selects silencer profiles (LD/MD/HD) based on target insertion loss and computes duct sizing.

New `App.tsx` appears to have added sliders for `targetEnclosureTemp` and possibly other previously-hidden parameters — needs full review of the new version to confirm.
</context>

<next_action>
1. Rename config files: `postcss_config.js` → `postcss.config.js`, `tailwind_config.js` → `tailwind.config.js`
2. Do full review of new `src/App.tsx` to catalog what changed vs archived version
3. Run `bun install && bun run dev` to verify app boots with styles
</next_action>
